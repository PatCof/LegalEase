{
  "version": 2,
  "builds": [
    {
      "src": "backend/gemini_ai.ts",
      "use": "@vercel/node"
    },
     {
        "src": "LegalEase/package.json",
        "use": "@vercel/static-build",
        "config": { "distDir": "build" }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/"
    }
  ],
  "buildCommand": "npm install --prefix LegalEase/backend && npm install --prefix LegalEase/LegalEase && npm run build --prefix LegalEase/LegalEase",
  "outputDirectory": "dist"
}